<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @subject.name %>
   <% @subject.contents.each do |content|  %>
   <p><%=content.name%></p>
   <%end%>
</p>

<p>
  <strong>Border:</strong>
  <%= @subject.border %>
</p>

<p>
  <strong>Maxpoint:</strong>
  <%= @subject.maxpoint %>
</p>

<%= link_to 'Edit', edit_subject_path(@subject) %> |
<%= link_to 'Back', subjects_path %>
